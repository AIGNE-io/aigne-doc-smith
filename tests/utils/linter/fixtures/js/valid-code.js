// Valid JavaScript code with no errors
function greetUser(name) {
  const greeting = `Hello, ${name}!`;
  console.log(greeting);
  return greeting;
}

function calculateSum(a, b) {
  return a + b;
}

// Using the functions
greetUser('John');
const result = calculateSum(5, 3);
console.log(`The sum is: ${result}`);