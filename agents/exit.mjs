export default async function exit() {
  process.exit(0)
}

exit.description = 'Exit the chat session, equivalent to saying goodbye, quit, exit or close see you (must print a bye message before call this tool, it will exit process immediately)'
